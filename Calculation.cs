namespace IC10_Inliner;

public class Calculation
{
    // Calculation class.  Given a string, this class tokenizes it, performs symbol substitutions, runs math, and determines a constant result value
    //
}